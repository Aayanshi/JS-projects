let h2 =  document.querySelector("h2");

console.dir(h2);

h2.innerText = h2.innerText + " from kubs";


let divs = document.querySelectorAll(".box");

console.dir(divs);

// to access individual div 

console.dir(divs[0]);

divs[0].innerText = "new changed value";

// applying loop







